type User @model @versioned {
  id: ID!
  name: String!
  avatar: String!
  dob: AWSDate
  address: String!
  latitude: Float
  longitude: Float
  description: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}
