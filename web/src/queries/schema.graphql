# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
    subscription: Subscription
}

type Mutation {
    createUser(body: UserRequest!): User!
    deleteUser(id: ID!): User!
    updateUser(body: UserRequest!, id: ID!): User!
}

type Query {
    user(id: ID!): User
    users(after: String, name: String): UserConnection!
}

type Subscription {
    userChanged: User
    userDeleted: User
}

type User {
    address: String!
    avatar: String!
    createdAt: AWSDateTime!
    description: String
    dob: AWSDate
    id: ID!
    lat: Float
    lng: Float
    name: String!
    updatedAt: AWSDateTime!
}

type UserConnection {
    list: [User!]!
    nextToken: String
}

input UserRequest {
    address: String!
    description: String
    dob: AWSDate
    lat: Float
    lng: Float
    name: String!
}


scalar AWSDate

scalar AWSDateTime