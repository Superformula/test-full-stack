{
    "version" : "2017-02-28",
    "operation" : "PutItem",
    "key" : {
        "id" : $util.dynamodb.toDynamoDBJson(${util.autoId()}),
        "name" : $util.dynamodb.toDynamoDBJson($ctx.args.name)
    },

    #set ( $insertObject = $util.dynamodb.toMapValues($ctx.args) )
    #set ( $insertObject.createdAt = $util.dynamodb.toString($util.time.nowISO8601()) )
    "attributeValues" : $util.toJson($insertObject)
}
