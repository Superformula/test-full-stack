{
    "version": "2017-02-28",
    "operation": "PutItem",
    "key": {
        "id": { "S": "$util.autoId()" }
    },
    "attributeValues": {
        "avatar": { "S": "https://source.unsplash.com/400x400/?people&v=$util.autoId()" },
        "name": $util.dynamodb.toDynamoDBJson($context.arguments.body.name),
        "dob": $util.dynamodb.toDynamoDBJson($context.arguments.body.dob),
        "address": $util.dynamodb.toDynamoDBJson($context.arguments.body.address),
        "lat": $util.dynamodb.toDynamoDBJson($context.arguments.body.lat),
        "lng": $util.dynamodb.toDynamoDBJson($context.arguments.body.lng),
        "description": $util.dynamodb.toDynamoDBJson($context.arguments.body.description),
        "createdAt": { "S": "$util.time.nowISO8601()" },
        "updatedAt": { "S": "$util.time.nowISO8601()" }
    }
}