type User @model @versioned {
  id: ID!
  name: String!
  avatar: String!
  dob: AWSDate
  address: String!
  lat: String
  long: String
  description: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}
