type User @model @versioned {
  id: ID!
  name: String!
  avatar: String!
  dob: AWSDate
  address: String!
  lat: Float
  long: Float
  description: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}
